# Environment variables for Kafka infrastructure
# Copy this file to .env and adjust values as needed

# Kafka Configuration
KAFKA_BROKER_ID=1
KAFKA_HEAP_OPTS=-Xmx1G -Xms1G

# Schema Registry Configuration
SCHEMA_REGISTRY_HEAP_OPTS=-Xmx512M -Xms512M

# Kafka Connect Configuration
CONNECT_HEAP_OPTS=-Xmx1G -Xms1G

# PostgreSQL Connection (for Kafka Connect sink)
# These should match your database/.env values
DB_HOST=postgres
DB_PORT=5432
DB_NAME=astro_telescope
DB_USER=postgres
DB_PASSWORD=postgres

# Kafka UI Configuration (optional)
KAFKA_UI_PORT=8080

# Topic Retention (in milliseconds)
# Commands/Acks: 7 days (604800000 ms)
# Data: 30 days (2592000000 ms)
# Status: 3 days (259200000 ms)
